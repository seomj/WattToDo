<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.wtd.backend.repository.CarbonConfigRepository">

    <select id="findValueByKey" resultType="float">
        SELECT config_value
        FROM carbon_config
        WHERE config_key = #{key}
    </select>

</mapper>