<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.wtd.backend.repository.CarbonRepository">

    <insert id="saveCarbonRecord" parameterType="com.ssafy.wtd.backend.model.CarbonRecord">
        INSERT INTO carbon_record (record_id, user_id, carbon_saved, created_at)
        VALUES (#{recordId}, #{userId}, #{carbonSaved}, NOW())
    </insert>

    <select id="getTotalCarbonSavedByUserId" resultType="java.lang.Float">
        SELECT COALESCE(SUM(carbon_saved), 0)
        FROM carbon_record
        WHERE user_id = #{userId}
    </select>

</mapper>